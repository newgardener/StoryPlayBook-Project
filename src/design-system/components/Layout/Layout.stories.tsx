import type { Meta, StoryObj } from "@storybook/react";

import { FlexLayout } from "./index";

export default {
  component: FlexLayout,
} as Meta<typeof FlexLayout>;

export const 기본: StoryObj<typeof FlexLayout> = {};
