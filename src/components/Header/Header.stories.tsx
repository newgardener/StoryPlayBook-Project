import type { Meta, StoryObj } from "@storybook/react";

import { Header } from "./index";

export default {
  title: "StoryPlayBook/컴포넌트/Header",
  component: Header,
} as Meta<typeof Header>;

export const 기본: StoryObj<typeof Header> = {};
